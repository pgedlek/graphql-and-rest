type Query {
    profiles: [Profile]
    profileById(id: Int): Profile
}

type Profile {
    id: ID
    firstName: String
    lastName: String
    email: String
    age: Int
    address: Address
}

type Address {
    id: ID
    city: String
    street: String
    state: String
    country: String
}
